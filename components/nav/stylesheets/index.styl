@require 'variables'

break = 768px

@require './persistent'
@require './overlay'
@require './dark'

.headroom
  transition transform 200ms linear

.headroom--pinned
  transform translateY(0%)

.headroom--unpinned
  transform translateY(-100%)
