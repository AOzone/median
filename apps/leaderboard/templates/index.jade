extends ../../../components/layout/templates/index
block vars
  - pageTitle = 'Leaderboard'
block body
  .container.container--leaderboard
    if accounts.length
      table.account-list.is-large
        thead
          tr
            td Name
            td Market Value
        tbody
          for account in accounts.models
            tr
              td(data-th="name"): a(href="/portfolio/#{account.get('trader_id')}") #{account.get('trader_id')}
              td(data-th="total market value") #{account.get('net_liquidation_value')}Å
    else
      .error.is-centered
        h3 No positions yet
